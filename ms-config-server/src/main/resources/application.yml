spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/filipve1994/microservices/
          search-paths:
            - "ms-config-properties/{application}/{profile}"
